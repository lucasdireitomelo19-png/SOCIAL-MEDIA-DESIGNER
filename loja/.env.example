# Banco de dados (SQLite local, arquivo criado automaticamente)
DATABASE_URL="file:./dev.db"

# NextAuth (gere um valor forte em produção, ex: `openssl rand -base64 32`)
NEXTAUTH_SECRET="troque-por-um-valor-aleatorio-forte"

# Credenciais do admin usadas apenas no seed inicial (troque a senha depois de logar)
ADMIN_EMAIL="admin@brecho.com"
ADMIN_PASSWORD="brecho123"
ADMIN_NAME="Administrador"
